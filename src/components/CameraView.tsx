import React from 'react';
import { View, Text } from 'react-native';

const CameraView = () => {
  return (
    <View>
      <Text>Camera View Component</Text>
    </View>
  );
};

export default CameraView;